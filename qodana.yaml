version: "1.0"
linter: jetbrains/qodana-jvm-community:latest
profile:
  name: qodana.recommended
failThreshold: 1
exclude:
  - name: Non Kotlin Files
    paths:
      - gradle
      - wrapper
      - specification
